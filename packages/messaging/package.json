{
  "name": "messaging",
  "version": "1.0.0",
  "description": "Shared messaging and event handling",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@nestjs/microservices": "^10.0.0",
    "redis": "^4.6.0"
  },
  "devDependencies": {
    "typescript": "^5.1.3"
  }
}
